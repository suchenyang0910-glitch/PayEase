## PayEase 安全响应策略（SECURITY-RESPONSE）

**版本**：v0.1-s0.1（引用基线 `docs/安全基线与上线验收清单.md §8.3 漏洞管理 SLA + §8.2 事件响应）

### 报告安全漏洞

- **邮件**：`security-report@[TODO: 替换为公司邮箱；上线前填入]`
  主题格式：`[SEC] 严重级别简评 - 受影响模块`
- **敏感数据（POC / 凭证）**：请使用 GPG 加密。公钥在 S0.2 `docs/SECURITY.asc` 上传。
- **禁止公开披露**：未修复完成前不得提交公开 Issue、PR 描述或群聊披露。

### SLA（基线 §8.3 摘要，CVSS v4.0 评级基准）

| 级别                  | 响应            | 修复                       | 复测责任人             |
| --------------------- | --------------- | -------------------------- | ---------------------- |
| Critical (CVSS ≥ 9.0) | **15 min 确认** | **24h 上线修复或临时缓解** | 安全负责人 + 模块Owner |
| High (≥ 7.0)          | 1h 确认         | 7 个工作日                 | 模块Owner              |
| Medium (≥ 4.0)        | 4h 确认         | 30 个工作日                | 模块Owner              |
| Low (< 4.0)           | 工作日内        | 90 个工作日                | 模块Owner              |

超过 SLA 未修复必须走 `SECURITY_EXCEPTIONS.yml` 双签字审批流程，临时缓解包括：

- WAF 规则、API 网关 deny list、特性开关关闭、IP 白名单、KMS 密钥轮转。

### 事件响应 5 步桥接（基线 §8.2 IR-01~04）

1. **T+15min 确认**：成立临时 IRT，划定业务/法规/对外沟通负责人。
2. **T+1h 遏制**：隔离凭证、轮换可能泄露的密钥/令牌（按基线 §7.3），关闭访问面；同步 Legal Hold 打开 DATA-05 DSR 工单。
3. **T+4h / T+24h 根除与恢复**：修复 root cause；灰度恢复 + 审计导出 WORM；对账务链执行 BIZ-05 日终平衡校验并标记异常批次。
4. **T+5d PM 报告**：时间线、根因、受影响数据量、修复、后续动作；向监管 / 合作方按 DPA 条款通报。
5. **T+7 天复盘**：更新威胁建模 TMD 与控制基线，加入 IR 桌面演练 5 场景。

### 允许的安全研究范围（Public Bug Bounty 尚未开放）

- 仅限 `payease.[TODO: 填入生产域名]` 旗下已上线的公共 API / Mini App。
- 禁止：DDoS / 大规模爬虫 / 社会工程 / 真实用户数据触碰 / 物理设备入侵；违者通报并配合执法。
- 若研究过程中访问到非公开用户数据，必须立即停止并邮件告知，且不得保留/分享。

### 漏洞例外审批（见 SECURITY_EXCEPTIONS.yml.template）

每项例外必须包含：CVE/描述、影响、临时缓解、过期日、工程Owner签字、安全负责人签字（四眼）。
到期前 7 天自动触发提醒，未续期视为阻断上线。
