# PayEase V1 上线验证记录（2026-08-18）

## 1. 发布版本与范围

- GitHub `main` 发布提交：`f51110a`
- 用户端、助贷运营后台、持牌机构后台、HR 门户、财务门户静态发布目录：`f51110a`
- API：`payease-preview-broker-api-1`，受控预览模式
- 本记录不包含真实客户资料、Telegram Bot Token、数据库口令、Basic Auth 凭据或任何 PII。

## 2. 代码与安全门禁

| 项目 | 证据 | 结论 |
| --- | --- | --- |
| 本地用户端测试 | `pnpm --filter @payease/user-mini-app run test` 连续两次，19 文件 / 100 测试通过 | 通过 |
| 本地构建 | `pnpm build`，共享包、API、用户端、助贷端、持牌端、HR、财务门户均完成 | 通过 |
| GitHub 质量门禁 | Actions run `32144178380`，提交 `f51110a` | 通过 |
| GitHub 本地安全门禁 | Actions run `32144178317`，提交 `f51110a` | 通过 |

## 3. 服务端候选预检与发布

发布前在 VPS 候选目录执行 API 预检，结果：

```json
{
  "ready": true,
  "authenticationRequired": true,
  "controlledPreview": true,
  "enabledBotCount": 2,
  "piiEncryptionRequired": true,
  "piiEncryptionReady": true,
  "piiActiveKeyVersion": "v1"
}
```

预检通过后，采用新目录与原子软链接切换。旧静态发布目录保留，API 使用 Docker Compose 重建；当前容器健康检查返回：

```json
{"status":"ready","service":"broker-api","storage":"postgresql"}
```

## 4. 外网烟雾验证

| 端点 | 结果 | 说明 |
| --- | --- | --- |
| `https://payease-user.khmerx.org/` | HTTP 200 | 用户 Mini App 静态资源可达 |
| `https://payease-broker.khmerx.org/` | HTTP 200 | 助贷运营端可达 |
| `https://payease-lender.khmerx.org/` | HTTP 200 | 持牌机构端可达 |
| `https://payease-hr.khmerx.org/` | HTTP 200 | HR 门户可达 |
| `https://payease-finance.khmerx.org/` | HTTP 200 | 财务门户可达 |
| `https://payease-api.khmerx.org/health/ready` | HTTP 401 | 符合 API 不直接公开的边界 |
| Caddy 配置 | `caddy validate` 通过 | 仅存在格式与弃用写法警告，未阻止加载 |

## 5. 匿名会话响应式基线检查

已在线上用户端执行 5 个宽度（320 / 360 / 375 / 390 / 414）× 3 个语言（km / en / zh-CN）× 浅色/深色的 30 组基础检查，覆盖：首页、全局底部导航、个人中心语言/主题控件。

- 未发现横向滚动：`scrollWidth <= innerWidth`
- 未发现可交互元素越出视口
- 中文切换后首页文案已确认实际渲染为中文
- 浏览器 console error：0
- 320 宽度下顶部消息与客服图标高度为 40px；这不是主 CTA，但建议在后续设计回归中评估是否统一提升至 44px。

## 6. 像素级验收状态

详见 [用户小程序页面像素级测试用例](用户小程序页面像素级测试用例.md)。

**状态：未完成，不得以本记录替代第 6 节的 30 条页面级验收。**

未完成的原因是当前公开匿名会话不具备该用例要求的固定测试数据与已验签 Telegram 会话：

1. 测试工厂、审核中申请、借款详情、账单、三条通知等稳定 seed 数据；
2. Telegram WebView 中的已验签测试账号；
3. 申请步骤 1–5、通知详情、借款详情、账单及错误态的可重复页面状态；
4. 每用例的截图基线与像素差异产物。

完成这些前置条件后，应按测试用例文件第 9 节执行并归档截图；若任何 VIS-001 至 VIS-024 发现 P0，必须回滚或修复后重验。
